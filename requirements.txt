# 核心依赖（已在 CLAUDE.md 登记）
jsonschema>=4.17.0
pytest>=7.0.0
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0
opencv-python>=4.8.0

# 可选依赖（按需启用）
# lerobot (Sprint 1.3+ VLA 训练)
# rosbags (rosbag 解析)
pyyaml>=6.0  # deployment-health-check: URDF/XML/YAML 配置解析
