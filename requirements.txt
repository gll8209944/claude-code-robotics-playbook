# Core dependencies (approved in CLAUDE.md)
jsonschema>=4.17.0
pytest>=7.0.0

# Optional: enable as needed per Sprint
# lerobot (Sprint 1.3+)
# pandas (data processing)
# numpy (numerical ops)
# pyarrow (parquet I/O)
# opencv-python (image validation)
# rosbags (rosbag parsing)
